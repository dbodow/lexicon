import * as apiCalls from './session_api_util';

// Integration testing:
// Actually sends the request to the Rails API

describe('signup', () => {
  context('when a response is successful', () => {
    it('has a correctly formatted user in the body', () => {

    });
  });

  context('when a response fails', () => {
    it('has the correct error in the body')
  });
});
